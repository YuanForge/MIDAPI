# NATS Server 配置
# max_payload: 单条消息最大字节数（字节）
# 图片生成渠道可能返回 base64 内联图片（数 MB），需调高此值。
max_payload: 62914560    # 60 MB（小于 max_pending 默认值 64MB）

# JetStream 持久化存储路径（与 docker run -v /opt/nats-data:/data 保持一致）
jetstream {
  store_dir: /data
}
